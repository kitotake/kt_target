// FIX: Toutes les constantes sont désormais définies dans src/config/index.ts.
// Ce fichier ré-exporte pour maintenir la compatibilité des imports existants.
export {
  RESOURCE_NAME,
  DEFAULT_NO_OPTIONS_LABEL,
  EYE_SVG_ID,
  CLICK_LOCKOUT_MS,
} from "../../../config";